# ── Python ────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so

# ── Virtual Environment ──────────────────────────────
.venv/
venv/
env/

# ── ML Models (download separately, see README) ─────
models/

# ── Data & Results (generated, not tracked) ──────────
datasets
DATASET_FVAB/
datasets/
results/
charts/

# ── Generated output files (never commit these) ──────
*_gaffe.json
*_analysis.json
*_gaffe_annotated.mp4
reference_face.jpg

# ── Test / scratch videos (too large for repo) ────────
# Keep only real dataset paths under datasets/
CLEANING/*.mp4
detection/*.mp4
detection/*.json

# ── OS / IDE ─────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# ── Jupyter ──────────────────────────────────────────
.ipynb_checkpoints/
